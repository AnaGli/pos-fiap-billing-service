apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service-publisher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-service-publisher
  template:
    metadata:
      labels:
        app: billing-service-publisher
    spec:
      containers:
        - name: publisher
          image: anaglislere/billing-service-tech-challenge:latest
          command: ["python", "-m", "app.messaging.outbox_publisher"]
          envFrom:
            - secretRef:
                name: billing-service-secrets
            - configMapRef:
                name: billing-service-config
